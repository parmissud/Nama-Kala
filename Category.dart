import 'package:flutter/cupertino.dart';

import 'Product.dart';
 class category {
  String title;
  List<Product> listProduct;
    String address;

  category(this.title, this.listProduct, this.address);
  Widget doFunc(category cat, bool log, String text) {

  }
}